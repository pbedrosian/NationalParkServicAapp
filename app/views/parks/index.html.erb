<!DOCTYPE html>
<html>
<head>
    <link rel="stylesheet" href="/stylesheets/page_styles.css">
</head>
<body>
<div class="topnav">
  <a class="active" href="/parks">Home</a>
  <a href="/parks/new">New Park</a>
  <a href="/account">Account</a>
  <a href="/logout">Logout</a>
</div><br>

<% if @parks.empty? %>
    <h1>Welcome <%=Helpers.name(session)%>, click below to add your first park:</h1>
    <br>
    <a href='/parks/new' class="myButton">Click To Get Started</a><br>
<% else %>
    <h1>Welcome <%=Helpers.name(session)%>, here are your visited parks:</h1><br>
    <% @parks.each do |p| %>
    <h3><%= p.name %></h3><br>
    <h4>Visited On: <%= p.date_visited %> </h4><br>
    <a href='/parks/<%=p.id%>' class="myButton">See Details</a>
    <br>
  <h3>  --------------------------------------------------- </h3>
    <% end %>
<% end %>


</body>
</html>